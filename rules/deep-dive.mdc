---
description: >
  Deep-dive a brief seed into a committed recommendation. Triggers on
  "deep dive:", "dig in:", "deep-dive", "what's the best way to",
  "think through this". Do not implement. "dig deeper" is do-it-right.
  Read the `deep-dive` skill.
alwaysApply: false
---

# Deep dive

Read the `deep-dive` skill. Do not reprint it here.

`dig in:` is this rule. `dig deeper` is `do-it-right`.

## House stops

- Do not implement. Do not write a `create-plan` artifact unless asked.
- Repo before the web on features and bugs. One self-critique pass, not a loop.
- User reply: `_shared/plain-english-brief.md`. Last output: next-prompt fence
  when the skill requires one.
