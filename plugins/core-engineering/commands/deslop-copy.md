---
description: Clean AI voice from user-facing prose without changing meaning.
---

Run the `cleaning-ai-copy` skill in full. Scope to named paths or the session
prose diff (`*.md` / `*.mdx`). Quote both scanner RECEIPTs. Do not invent a
"% AI" score.

Distinct from `deslopping` (code slop) and `simplifying-code` (YAGNI). This
command is `/deslop-copy`. `/deslop` is not this command.

Focus / paths: $ARGUMENTS
