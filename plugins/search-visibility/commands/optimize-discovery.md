---
description: Implement technical and on-page discovery fixes. Quote both scanner RECEIPTs.
---

Run the `optimizing-for-discovery` skill in full. Scope to named public
pages or live URLs. Quote the baseline and re-scan RECEIPTs. Named URLs
must show `source: url`. Do not promise a ranking or citation.

Distinct from `auditing-search-visibility` (report) and
`writing-citable-content` (prose). This command is `/optimize-discovery`.

Focus / paths: $ARGUMENTS
